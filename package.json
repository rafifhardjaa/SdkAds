{
  "name": "sdk-ads-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:sdk": "npm run build --workspace=@sdk-ads/sdk",
    "build:backend": "npm run build --workspace=@sdk-ads/backend",
    "start:backend": "npm run start --workspace=@sdk-ads/backend"
  }
}
